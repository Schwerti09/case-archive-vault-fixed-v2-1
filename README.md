# Case Archive Vault (Epstein Case Index) — v0.2.6

Free Index + Vault Subscription (€4,99/month).

## Local
npm i
cp .env.example .env
npm run db:push && npm run db:seed
npm run dev

## Deploy (Netlify)
- uses @netlify/plugin-nextjs in netlify.toml
- set ENV vars in Netlify UI

## Ops
/ops/login (OPS_PASSWORD)
